<%= form_for(@spin) do |f| %>
  <% if @spin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spin.errors.count, "error") %> prohibited this spin from being saved:</h2>

      <ul>
      <% @spin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
