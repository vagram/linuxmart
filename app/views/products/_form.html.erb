<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :platform_id %><br />
    <%= f.collection_select :platform_id, Platform.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :distro_id %><br />
    <%= f.collection_select :distro_id, Distro.all, :id, :name %><br />
  </div>
  <div class="field">
    <%= f.label :distro_version_id %><br />
    <%= f.collection_select :distro_version_id, DistroVersion.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :spin_id %><br />
    <%= f.collection_select :spin_id, Spin.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
