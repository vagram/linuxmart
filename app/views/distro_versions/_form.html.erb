<%= form_for(@distro_version) do |f| %>
  <% if @distro_version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@distro_version.errors.count, "error") %> prohibited this distro_version from being saved:</h2>

      <ul>
      <% @distro_version.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :human_name %><br />
    <%= f.text_field :human_name %>
  </div>
  <div class="field">
    <%= f.label :issued_on %><br />
    <%= f.date_select :issued_on %>
  </div>
  <div class="field">
    <%= f.label :distro_id %><br />
    <%= f.collection_select :distro_id, Distro.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
